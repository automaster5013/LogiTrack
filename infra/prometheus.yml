global:
  scrape_interval: 10s
rule_files:
  - /etc/prometheus/prometheus-alerts.yml
scrape_configs:
  - job_name: logitrack-api
    metrics_path: /actuator/prometheus
    static_configs:
      - targets: ["api:8080"]
